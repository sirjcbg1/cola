<form method="post" action="{{ path('app_cola_delete', {'id': cola.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cola.id) }}">
    <button class="btn">Delete</button>
</form>
